name: Build And Release Binaries

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.label }}
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Linux x86_64
            runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: ntfs-share-wizard-linux-x86_64
            binary_name: ntfs-share-wizard-linux-x86_64
            binary_suffix: ""
          - label: Windows x86_64
            runner: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: ntfs-share-wizard-windows-x86_64
            binary_name: ntfs-share-wizard-windows-x86_64.exe
            binary_suffix: .exe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install stable Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Organize release binary
        shell: bash
        run: |
          mkdir -p "dist/${{ matrix.artifact_name }}"
          cp "target/${{ matrix.target }}/release/ntfs-share-wizard${{ matrix.binary_suffix }}" \
            "dist/${{ matrix.artifact_name }}/${{ matrix.binary_name }}"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Show downloaded files
        run: find dist -maxdepth 3 -type f | sort

      - name: Create GitHub release and upload assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            dist/ntfs-share-wizard-linux-x86_64/ntfs-share-wizard-linux-x86_64 \
            dist/ntfs-share-wizard-windows-x86_64/ntfs-share-wizard-windows-x86_64.exe \
            --title "${GITHUB_REF_NAME}" \
            --generate-notes
